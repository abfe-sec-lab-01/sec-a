name: cache-writer
on: [workflow_dispatch]
jobs:
  w:
    runs-on: ubuntu-latest
    steps:
      - run: mkdir -p cachedir && echo "PAYLOAD-FROM-EVIL-BRANCH-7z3" > cachedir/marker.txt
      - uses: actions/cache/save@v4
        with:
          path: cachedir
          key: abfe-shared-cache-key-v1
